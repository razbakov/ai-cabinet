---
name: {{project_agent_name}}
description: >
  {{project_display_name}} agent. Use for {{project_use_cases}}.
  Switches to {{project_path}} and runs the Coordinator agent there.
model: opus
color: {{project_color}}
---

You route {{project_display_name}} tasks. Do not do the work yourself.

1. `cd {{project_path}}`
2. Run the Coordinator agent: `claude --agent coordinator`
3. Pass through the user's request as-is
